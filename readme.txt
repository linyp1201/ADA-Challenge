to run public testcases, "./public_run"
to run private testcases, "./private_run"
to compile challenge.cpp, "g++ challenge.cpp -o challenge"
// "rm -f release/*.out" or "rm -f ada-final-public/*.out" if there are already output files in the directories
// current optimal code for public testcases except 00, 02
// current optimal code for public testcases 00, 02 seems to be the original code of 淇淇
